# Data Model: Fix Nuxt Docker Build Error

**Feature**: 005-fix-nuxt-build
**Date**: 2026-01-09

## Overview

This feature is a **build infrastructure fix** and does not introduce new data models or modify existing application data structures.

## Data Entities

### No Changes

This feature does not affect:
- Application data models (Articles, Speckits, etc.)
- Database schemas
- API response structures
- State management stores (Pinia)
- Component state or props

## Build Artifacts

### Docker Build Context

The following artifacts are generated or modified during the Docker build process:

| Artifact | Location | Purpose | Modified? |
|----------|----------|---------|-----------|
| **Server Bundle** | `.output/server/index.mjs` | Main server entry point | Generated by Nuxt build |
| **Client Assets** | `.output/public/*` | Static files (JS, CSS, images) | Generated by Nuxt build |
| **Build Metadata** | `.output/server/chunks/*` | Lazy-loaded code chunks | Generated by Nuxt build |
| **Docker Image** | `registry/project:tag` | Production container image | Created by Docker build |

### Configuration Files (Modified)

| File | Purpose | Changes |
|------|---------|---------|
| `Dockerfile` | Multi-stage build configuration | Remove incorrect `node_modules` copy, add `.nuxt` cleanup |
| `.dockerignore` | Exclude unnecessary files from build | Create if not exists |

## Dependencies

### Nuxt 3 Internal Modules (Implicit)

The fix ensures these internal Nuxt modules are properly bundled:
- `#internal/nuxt/paths` - Path resolution utilities
- `#internal/nuxt/kit` - Nuxt developer kit
- `#internal/nuxt/vite` - Vite integration
- Other subpath imports used by Nitro server

These modules are **bundled automatically** by the Nuxt build process when configured correctly.

## Summary

**Data Model Impact**: ✅ **NONE**

This feature exclusively addresses Docker build infrastructure and does not require data model changes.
